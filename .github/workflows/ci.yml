name: CI

on:
  pull_request:
  push:
    branches: [main, dev]

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sanity check
        run: echo "CI sanity check passed"

